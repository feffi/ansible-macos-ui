---
galaxy_info:
  author: Kevin Wennemuth
  description: Ansible role to manage UI settings in macOS.
  license: MIT
  min_ansible_version: 2.3
  platforms:
    - name: Darwin
      versions:
        - all
  galaxy_tags:
    - system
    - osx
    - config
    - ui
dependencies:
  - { role: feffi.macos-defaults,
      macos_defaults: {
        defaults: []
      }
    }